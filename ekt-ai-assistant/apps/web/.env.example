# Application backend origin only. Empty means same origin /api/*.
NEXT_PUBLIC_API_BASE_URL=
# Explicitly enable fixtures; never automatically falls back from live errors.
NEXT_PUBLIC_DEMO_MODE=false
# Enable only after teammates approve the local structured response extensions.
NEXT_PUBLIC_UI_EXTENSIONS=false
